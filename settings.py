# MySQL Database Configuration
# Update these values to match your local MySQL setup

DB_HOST = "localhost"  # Usually 'localhost' for local development
DB_USER = "root"       # Your MySQL username (commonly 'root' for local development)
DB_PASSWORD = ""       # Your MySQL password (leave as empty string "" if no password is set)
DB_NAME = "medico_db"  # Database name (will be created automatically by setup_db.py)
DB_PORT = 3306         # Default MySQL port (change only if you use a different port)